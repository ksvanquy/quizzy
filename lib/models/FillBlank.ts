import { QuestionModel } from '@/infrastructure/persistence';

// Legacy alias: fill-blank questions are stored in the Question collection
export const FillBlank = QuestionModel;
export default FillBlank;
