import { QuestionModel } from '@/infrastructure/persistence';

// Legacy alias: ordering questions are stored in the Question collection
export const Ordering = QuestionModel;
export default Ordering;
