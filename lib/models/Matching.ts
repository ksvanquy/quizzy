import { QuestionModel } from '@/infrastructure/persistence';

// Legacy alias: matching questions are stored in the Question collection
export const Matching = QuestionModel;
export default Matching;
