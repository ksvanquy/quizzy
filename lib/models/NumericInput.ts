import { QuestionModel } from '@/infrastructure/persistence';

// Legacy alias: numeric-input questions are stored in the Question collection
export const NumericInput = QuestionModel;
export default NumericInput;
